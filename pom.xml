<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.example</groupId>
    <artifactId>ConsolePeek</artifactId>
    <version>1.1.0</version>
    <packaging>jar</packaging>

    <properties>
        <!-- Match this to the Java version your server runs on.
             Minecraft 26.1 requires Java 25. If your server is older
             (e.g. 1.21.x on Java 21) change this to 21. -->
        <maven.compiler.release>25</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>

    <repositories>
        <repository>
            <id>papermc</id>
            <url>https://repo.papermc.io/repository/maven-public/</url>
        </repository>
    </repositories>

    <dependencies>
        <dependency>
            <groupId>io.papermc.paper</groupId>
            <artifactId>paper-api</artifactId>
            <!-- Versioning changed in 26.1. Two cases:
                 * 26.x server: use a build range so Maven grabs the latest build,
                   e.g. [26.2.build,) for a 26.2 server, [26.1.build,) for 26.1.x.
                   You can also pin an exact build like 26.2.build.60-beta.
                 * 1.21.11 or older: use the old format, e.g. 1.21.4-R0.1-SNAPSHOT -->
            <version>[26.2.build,)</version>
            <scope>provided</scope>
        </dependency>
    </dependencies>

    <build>
        <finalName>${project.artifactId}</finalName>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
            </plugin>
        </plugins>
    </build>
</project>
